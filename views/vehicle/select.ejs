<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Select Vehicle</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #6a11cb, #2575fc);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 400px;
            text-align: center;
        }
        h1 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #343a40;
        }
        label {
            display: block;
            margin: 15px 0;
            font-size: 1.2rem;
            color: #495057;
        }
        button {
            padding: 15px 30px;
            font-size: 1rem;
            color: #ffffff;
            background-color: #2575fc;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0e47cb;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Select Vehicle</h1>
        <form action="/vehicle/select" method="post" style="font-weight: bold;">
            <input type="hidden" name="user_id" value="<%= user_id %>">
            <label>
                <input type="radio" name="vehicle_choice" value="existing"> Existing Vehicle
            </label>
            <label>
                <input type="radio" name="vehicle_choice" value="new"> New Vehicle
            </label>
            <button type="submit">Continue</button>
        </form>
    </div>
</body>
</html>
