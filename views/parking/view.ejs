<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Parking Slots - SmartPark</title>
  <style>
    body {
      font-family: 'Roboto', Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(to right, #6a11cb, #2575fc); /* Gradient background */
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      height: 100vh;
    }
    .container {
      width: 80%;
      margin: 20px auto;
      background: rgba(255, 255, 255, 0.9);
      padding: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-around;
    }
    h1 {
      text-align: center;
      color: #333;
      width: 100%;
      margin-bottom: 20px;
    }
    .slot {
      background-color: #e0f7fa;
      border: 1px solid #00838f;
      border-radius: 8px;
      width: 18%;
      margin: 10px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      transition: transform 0.2s;
    }
    .slot:hover {
      transform: scale(1.05);
    }
    .slot.no {
      background-color: #ffebee;
      border-color: #c62828;
    }
    .slot.yes {
      background-color: #e8f5e9;
      border-color: #12b91a;
    }
    .slot h2 {
      margin: 0;
      color: #333;
    }
    .slot p {
      margin: 5px 0 0;
      color: #555;
    }
    .back-button {
      background-color: #4285f4;
      color: white;
      padding: 15px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      transition: background-color 0.3s;
      margin: 20px auto;
      display: block;
    }
    .back-button:hover {
      background-color: #3367d6;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Parking Slots</h1>
    <% parkingSlots.forEach(slot => { %>
      <div class="slot <%= slot.Availability.toLowerCase() %>">
        <h2>Slot No: <%= slot.SlotNo %></h2>
        <p>Availability: <%= slot.Availability %></p>
        <p>User ID: <%= slot.user_id %></p>
      </div>
    <% }); %>
  </div>
  <form action="/admin/dashboard" method="get">
    <button type="submit" class="back-button">Back</button>
  </form>
</body>
</html>